# Active Implementation Roadmap

## Status and authorization

The owner-authorized current phase is **v1.1 Allowlisted Self-Signup + Profile Password and Display Name Update**, defined in `V1_1_AUTH_PLAN.md`, including the 2026-08-31 Profile/Auth UI refinement now owner-approved, committed, and deployed. The deployed checkpoint is `main` at `b717167`; Appwrite deployment `6a9597c75f246e9bce4f` became Ready/Active at the unchanged production origin. Live Schema V5 differs from V4 only by `profiles.displayName` capacity 64 -> 16,383 with metadata advanced last after provider availability; the refinement adds a product-only 1–20 trimmed Profile Display Name rule without schema mutation. Third-account creation, real password or Display Name mutation, Appwrite user-limit changes, and the v1.1.0 tag remain separately gated. R5 production evidence remains historical baseline context.

### v1.1 authentication phase — authorized 2026-08-30

**Entry:** production v1.0.1 was deployed; the owner supplied and authorized the controlled v1.1 authentication contract and its Display Name extension. **Current checkpoint on 2026-08-31:** a fresh external verified backup preceded the approved live Schema V5 widening; only `profiles.displayName` 64 -> 16,383 was applied, provider availability was verified, metadata advanced 4 -> 5 last, and the planner remains zero-drift. The Display Name UI/command preserves Profile identity, keeps Password separate, refreshes live Profile-derived projections, and never rewrites historical snapshots. It is checkpointed and deployed from `main`. STOP before any real old-name -> new-name mutation, third-account creation, real password mutation, user-limit change, or tagging.

**Verification result:** focused v1.1 extension 147/147; post-deployment full Vitest 756/756 across 95 files; architecture 16/16; ESLint, TypeScript, Next.js production build, zero-vulnerability audit, diff-check, and built-client secret scan green. Explicit local composition passed Chromium 74/74 and Firefox/WebKit smoke 1/1 each. Production anonymous auth surfaces passed Chromium/Firefox/WebKit 3/3 at 430/390/360 with Axe serious/critical zero, no hydration errors, exact disallowed/existing-account responses, no public allowlist/server secrets, and unavailable verification/email-edit routes. Auth/Profile totals remain 2/2, the existing account and business/history rows are unchanged, one approved account remains unused, and live Schema V5 re-plan is zero-drift. A browser-managed authenticated production session was unavailable, so real Profile no-op/Password UI mutation checks remain owner-gated rather than inferred; no real name, password, or account mutation occurred.

**Profile/Auth UI refinement result (2026-08-31):** read-only production compatibility passed for all 2 Profiles (maximum trimmed length 6; zero above 20). The Profile-only 1–20 trimmed rule now spans UI/request/application/domain/authoritative persistence boundaries; shared independent password visibility controls cover Login, Signup, and Profile; Display Name controls align exactly; Account/Current Household cards stretch equally side-by-side; and Profile password controls share one bounded responsive width. Focused 90/90; full Vitest 763/763 across 97 files; architecture 16/16; ESLint, TypeScript, production build, audit, diff, and client-secret/password-log scans green; explicit-local Chromium 75/75; production-anonymous Chromium 3/3; Firefox/WebKit smoke 1/1 each; responsive/Axe matrix green at 1440/1024/768/430/390/360. The approved implementation is commit `b717167`; CI passed and deployment `6a9597c75f246e9bce4f` reached Ready/Active with edge distribution 6/6. Live Login/Signup responsive toggles and authenticated Profile toggles/layout passed without a mutation or application console/hydration error. Schema V5/V4 definitions, authentication semantics, and financial/Household behavior are unchanged; owner acceptance remains pending and no v1.1.0 tag was created.

## Reconciliation findings

No direct contradiction exists between the original AIDOS constraints and the frozen context. One documentation mismatch was corrected: the app is a shared household tracker, not merely a personal finance app.

The following implementation-critical choices were approved as frozen clarifications.

1. **Split remainder order** - Use canonical ascending participant ID order for deterministic equal-split remainder poisha and as the final tie-break for percentage rounding.
2. **Percentage precision** - Accept up to two decimal places and store percentage units as integer basis points (`100% = 10,000`). Convert to poisha with largest-remainder allocation; tie-break by participant ID.
3. **Settlement optimization** - Use deterministic largest-debtor/largest-creditor matching, ordered by absolute balance then member ID. It yields at most `members - 1` transfers; do not claim a globally minimal transfer count unless a later requirement explicitly demands it.
4. **Stale pending settlements** - Snapshot sender, receiver, amount, and originating recommendation at creation. Confirmation records the externally received payment even if expenses later changed; because the receiver confirms a real payment, its exact amount affects balances and may create a new reverse balance. Show a stale-balance warning before confirmation when current obligation differs.
5. **Leader editing a Card-paid expense** - A non-owner leader may preserve the opaque existing Card payment reference and edit permitted non-card fields, but cannot see/select another user's card or change private card metadata. Changing Card selection is owner-only; changing payment method needs explicit confirmation that private metadata will be detached.
6. **Former-member history** - Freeze amount, payer, participants, shares, date, payment method, and deletion for any expense involving a former member when the action would change their settled zero position. Permit only non-financial metadata edits that preserve financial results. Raise ambiguous cases.
7. **Deleting referenced cards** - Soft-delete/archive the card. Existing expenses retain an owner-private historical label snapshot/reference; the card is unavailable for new expenses.
8. **Local persistence** - Use IndexedDB behind repository interfaces for structured local data and receipt blobs; keep a seed/reset development adapter for repeatable tests. This is a replaceable technical choice, not a production backend commitment.
9. **Dates and month boundaries** - Store expense dates as date-only `YYYY-MM-DD`; calculate month membership without UTC conversion. Store audit timestamps as ISO instants. Display in BDT/English initially unless approved UI references require Bengali localization.
10. **Local authentication boundary** - Build all required auth screens and state transitions against a local auth repository, including seeded identity switching. Real verification emails, password delivery, security, and production sessions are deferred to Appwrite integration; local UI must clearly remain development-only.
11. **Money formatting boundary** - Keep exact canonical money conversion in the domain as ungrouped decimal text. Currency symbols, Bangladesh digit grouping, and other display localization belong outside the domain and cannot participate in financial arithmetic.
12. **Zero-share participants** - A selected participant may receive zero poisha when exact deterministic allocation requires it. Completed allocations retain every selected participant exactly once, including zero-share participants.
13. **Unordered Pending uniqueness** - For one household, allow at most one Pending settlement for the same unordered member pair. Same-direction and reverse-direction duplicates are blocked until the existing claim becomes terminal; terminal history does not block later creation.
14. **Sole-leader exit** - A sole remaining leader cannot leave or trigger automatic deletion. Explicit household deletion is the only exit path after every deletion gate passes.
15. **Leadership transfer finance boundary** - Leadership transfer is an authority change and does not require zero balances. It must transfer from the current leader to another active member and preserve exactly one active leader; later leave still requires zero balance and no Pending settlements.
16. **Settlement creation boundary** - A new settlement must exactly match a current full deterministic recommendation. Its Pending snapshot then retains the original parties and amount regardless of later balance or recommendation changes.

All sixteen items are approved. Reopening one requires change analysis under the AIDOS requirement-change workflow.

## Model/reasoning scale

- **High** - GPT-5 Codex with high reasoning for financial invariants, architecture boundaries, privacy/security, migrations, and multi-module debugging.
- **Medium** - GPT-5 Codex with medium reasoning for contained feature/UI implementation with established patterns.
- **Low** - GPT-5 Codex with low reasoning only for narrow mechanical work after decisions and patterns are fixed.

## Phased roadmap

### Phase 1 - Project foundation

Set up Next.js/TypeScript, Tailwind, shadcn/ui, lint/typecheck, Vitest/RTL, Playwright, path boundaries, environment conventions, and the local-only development shell - without product features. Define repository contracts at a skeleton level and architecture guard tests. **Reasoning: medium-high** because early boundaries affect every phase. Exit: clean build, lint, typecheck, unit smoke test, and Playwright smoke page.

**Result: complete (2026-08-12).** Established Next.js 16.3 App Router, React 19.2, strict TypeScript, Tailwind 4, shadcn/Radix/Lucide foundations, layer skeletons and architecture guards, Vitest/RTL, Playwright Chromium, Git `main`, and concise project commands. No Appwrite, IndexedDB adapter, financial/domain logic, or product feature was added.

**Verification:** dependency audit reported zero vulnerabilities; lint passed; typecheck passed; production build passed; Vitest passed 5 tests across 2 files; architecture guard passed 4 layer cases; Playwright passed the Chromium foundation flow with explicit meaningful-content and no-error-overlay assertions; local server returned HTTP 200 with no logged runtime error.

### Phase 2 - Domain model and exact money engine

Define IDs/entities/value types, integer-poisha parsing/formatting, date-only handling, split inputs, invariants, and Zod boundary schemas. Implement equal, amount, and percentage split logic as pure functions after decisions 1-2 are approved. **Reasoning: high** due financial correctness and rounding. Exit: exhaustive deterministic unit/property-style boundary tests with no React/storage imports.

**Result: complete (2026-08-12).** Added branded safe-integer poisha and basis-point values, strict decimal-string parsing, deterministic ungrouped canonical BDT conversion, date-only Gregorian validation, opaque IDs with code-unit ordering, completed financial input types, and pure equal/amount/percentage allocation. Equal remainders use stable participant-ID order; percentage allocation uses BigInt largest-remainder calculations with stable-ID ties. Exact allocations retain every selected participant exactly once, including zero shares. Zod exists only at the application boundary.

**Verification:** focused Phase 2 suite passed 119 tests across 9 files; full Vitest passed 124 tests across 11 files; lint passed; TypeScript passed; production build passed; architecture guards passed and explicitly prohibit Zod/framework/storage-facing imports from the domain. Source audit found no floating-point parsing, rounding, locale formatting, or number-based financial multiplication/division in Phase 2 code.

### Phase 3 - Balance, settlement, membership, and permission engines

Implement balance signs, confirmed-settlement effects, recommendation algorithm, lifecycle rules, duplicate protection, leave/remove/transfer/delete gates, former-member safety, expense permissions, and card-privacy projections after decisions 3-6 are approved. **Reasoning: high** due temporal finance, privacy, and cross-domain invariants. Exit: scenario matrix tests proving zero-sum balances and lifecycle/permission behavior.

**Result: complete (2026-08-12).** Added pure exact-poisha household ledgers with positive-creditor/negative-debtor convention; confirmed-only settlement effects; deterministic largest-debtor/largest-creditor recommendations with stable-ID ties; exact-current-recommendation settlement creation; immutable lifecycle transitions; stale-claim assessment; household-scoped unordered-pair Pending uniqueness; creator/leader/member expense permissions; private-card projection/edit capabilities; conservative former-member financial fingerprints; and leave/remove/leadership-transfer/household-deletion gates. No persistence, UI, auth, Appwrite, receipt, card CRUD, dashboard, or Phase 4 work was introduced.

**Verification:** focused Phase 3 suite passed 78 tests across 12 files; full Vitest passed 202 tests across 23 files; lint passed with zero warnings; TypeScript passed; architecture guards passed; production build passed; Playwright passed and exited normally using the established Windows teardown permission; dependency audit reported zero vulnerabilities. Deterministic property-style tests prove generated ledgers remain exactly zero-sum under reordered inputs and generated recommendations resolve valid balance sheets exactly. Source audit found no floating-point financial parsing, rounding, localization, multiplication, or division.

### Phase 4 - Local application layer and persistence

Implement application services, repository interfaces, IndexedDB/local repositories, seed/reset fixtures, audit events, receipt-blob lifecycle, local auth state, and identity switching. No Appwrite SDK. **Reasoning: high** because persistence and privacy projections must preserve domain boundaries. Exit: repository contract tests, reload persistence, deterministic seeds, and no private-card leakage across identities.

**Result: complete (2026-08-13; committed as `76a9ef6`).** Added domain records and validators for profiles, households, join requests, expenses, cards, private historical card snapshots, receipts, and audit events; application-owned repositories and provider-independent services; explicit atomic local operations; IndexedDB schema/records/mappers/repositories using `idb`; `fake-indexeddb` integration coverage; optional derived uniqueness keys; owner-private card history; validated JPEG/PNG/WebP Blob storage; append-only audit records; development session switching; deterministic seed/reset; typed blocked/newer-version failures; and client-only Next.js composition. Household deletion atomically tombstones the household, retains every membership as former history, and releases active-membership uniqueness keys. No UI, Appwrite, API persistence, derived financial store, or Phase 5 work was added.

**Verification:** Phase 4 focused tests passed 28 tests across 2 integration files; architecture guards passed 6 cases; full Vitest passed 232 tests across 25 files, preserving all Phase 2/3 coverage; lint passed with zero warnings; TypeScript passed; production build passed and retained a statically prerendered root route; Playwright Chromium smoke passed 1 test; dependency audit reported zero vulnerabilities; and `git diff --check` passed. Tests cover close/reopen persistence, transaction rollback, optional derived uniqueness, malformed-record rejection, receipt byte round trips/tombstones, private-card projection and opaque leader edits, referenced-card archival, confirmed-settlement immutability, former-member/soft-deleted-expense retention, deterministic seed/reset, identity switching, schema/version failures, and absence of derived financial stores.

**Known verification boundary:** `fake-indexeddb` is the primary repository environment and is not identical to native browser IndexedDB. No product/test-only route was added solely for infrastructure testing. Native-browser repository verification remains an integration check when a later authorized UI/runtime phase first consumes `LocalDevelopmentRuntime`; existing Playwright smoke verifies that the browser build and Server Component boundary remain healthy.

### Phase 5 - Application shell and design system

Build responsive desktop sidebar/mobile navigation, route shell, tokens, typography, buttons, inputs, cards, status treatments, dialogs/sheets, skeletons, toasts, and accessibility primitives from the canonical design baseline. **Reasoning: medium** because product logic is low but consistency is broad. Exit: reusable component showcase/routes pass visual, keyboard, responsive, and contrast checks.

**Result: complete (2026-08-13; committed as `66558ab`).** Added the frozen Soft Premium Finance token system and Inter scale; statically prerendered Server Component routes; root redirect to Dashboard; sparse placeholders; a responsive shell with full sidebar at `>=1024px` and safe-area-aware bottom navigation below it; accessible mobile More sheet; understated House wordmark; application-service-backed current profile/role; intentionally unavailable explained Log Out control; deterministic initials; project surfaces, metric/chart/state/status/member/form/money/confirmation primitives; Sonner infrastructure; and a separate development-only identity toolbox. One client composition root privately owns the local runtime and exposes only presentation session view state. Runtime acquisition is shared across Strict Mode remounts and handles abandoned initialization, retry without reset, session refresh, pagehide/unmount, and connection closure. No feature screen, authentication, Appwrite, analytics, Recharts, or deployment work was introduced.

**Verification:** full Vitest passed 246 tests across 28 files; focused presentation/application/architecture coverage passed; architecture guards passed 10 cases; lint passed; TypeScript passed; production build passed with all eight product routes statically prerendered; Playwright Chromium passed 9 shell/runtime checks across mobile, large-mobile, tablet, laptop, and desktop widths; native IndexedDB opened, seeded, switched identity, closed, reopened, retained the selected identity, and reconstructed profile/role through application services; keyboard skip-link and sheet focus-return checks passed; reduced-motion checks passed; serious/critical Axe findings are zero; browser console/page/hydration checks passed; visual review at 390x844 and 1440x900 matched the approved sparse shell direction; dependency audit reported zero vulnerabilities; and `git diff --check` passed.

### Phase 6 - Local session and household onboarding

Use the existing development session as the local authenticated-user equivalent; add household-aware startup, create/join/cancel/accept/reject flows, and pre-acceptance privacy boundaries without fake authentication UI. **Reasoning: high** due permission, privacy, transaction, and runtime-state boundaries. Exit: multi-user Playwright onboarding flows and proof that pending requesters cannot access household data.

**Result: complete and accepted (2026-08-13; committed as `f5719b9`).** Added presentation-safe household access states; centrally configured membership guards for Dashboard, Expenses, Add Expense, and Settlements; independent Household/Profile/Cards routing; React Hook Form + Zod create/join forms; exact leading-zero code handling; secure injected code generation with 32 bounded uniqueness attempts; minimal pre-acceptance lookup projection; Pending cancellation; leader Accept/Reject confirmations; and centralized authoritative runtime reconstruction after every mutation and development identity change. Named IndexedDB transactions now recheck active membership and Pending constraints during create/join and recheck membership during atomic acceptance. Terminal request/audit history and the Phase 4 seed remain unchanged. No login/password/authentication, financial feature, full member management, Appwrite, or Phase 7 work was added.

**Verification:** full Vitest passed 264 tests across 31 files; Phase 6 application/component coverage includes no-household, leading-zero/manual/generated/duplicate codes, 32 collision retries, one-house/Pending gates, lookup privacy, cancellation and terminal history, leader/non-leader/wrong-house authorization, exactly-one membership, and forced transaction rollback. Architecture guards passed 11 cases; lint, TypeScript, production build, and `git diff --check` passed; all 11 routes remained statically prerendered. Playwright Chromium passed 17 tests, including native IndexedDB close/reopen persistence, cross-identity create/join/accept/reject, approved route gating, mobile/tablet/desktop layouts, keyboard focus return, no overflow, zero serious/critical Axe findings, and no console/page/hydration errors. Dependency audit reported zero vulnerabilities.

### Phase 7 - Expenses and receipts

Build expense list/search/filter/sort, single-page add form with live allocation summary, owner-only card selection, local receipt previews/storage, details/history, edit, and soft delete. **Reasoning: high** because this joins forms, money, privacy, audit, and recalculation. Exit: all split modes and edit/delete permissions pass unit, component, and multi-user browser tests.

**Result: complete and accepted (2026-08-18; committed as `ff5a878`).** Added the household-only Expense list with exact local-month/search/payer/payment/sort composition; responsive Add/Edit/Details screens; strict application/domain split previews and persistence; owner/leader/member permissions; privacy-safe Card projections and edit transitions; staged atomic receipt additions/removals; retained receipts on expense soft deletion; safe activity; explicit former/legacy read-only financial states; and transactional membership-race rechecks. Expense record version `2` and IndexedDB schema version `2` persist modern percentage basis-point source entries alongside canonical poisha allocations. The migration preserves v1 allocations exactly and marks missing percentage source as non-reconstructable legacy history without fabricating values.

**Verification:** focused migration/domain/application/persistence suites passed; full Vitest passed 296 tests across 34 files; architecture guards passed 11 cases; lint and TypeScript passed; production build passed with dynamic Details/Edit routes and all other approved routes; full Playwright Chromium passed 21 flows, including four Phase 7 product flows, native IndexedDB reload, responsive breakpoints, keyboard navigation, no private Card strings for non-owners, and zero serious/critical Axe findings. Dependency audit found zero vulnerabilities. `git diff --check` passed. Manual source audits found no presentation persistence imports, no private Card reference fields in presentation, no expense-date UTC round trips, and no new floating-point financial arithmetic. Phase 8 was not started.

### Phase 8 - Settlements

Build recommendations, external-payment disclosure, pending/confirm/reject/cancel flows, badges, duplicate prevention, stale warnings, and immutable history. **Reasoning: high** due financial lifecycle and concurrency-like behavior. Exit: sender/receiver Playwright matrix and exact balance assertions for every status.

**Plan status (2026-08-18): approved and authorized.** Exact scope, exclusions, route/component hierarchy, application projections/use cases, derived-state rules, actor-specific Pending lifecycle, stale/duplicate behavior, History, cross-identity refresh, responsive/accessibility behavior, proposed files, test matrix, risks, and reasoning recommendation are documented in `PHASE_8_PLAN.md`. Approved clarifications require browser-local English instant formatting and authoritative atomic revalidation of the exact current recommendation before Pending creation. Phase 9 remains prohibited.

**Result: complete and accepted (2026-08-18; committed as `c75d18b`).** Replaced the Settlements placeholder with current-user net-position summaries, deterministic incoming/outgoing recommendations, exact full-recommendation Settle Up disclosure, actor-specific Pending cards, fresh plain-language stale previews, receiver Confirm/Reject, sender Cancel, household-wide terminal History, viewer-local English instant formatting, live feedback, and receiver-action navigation badges. Application-owned projections keep financial/lifecycle/staleness logic out of React. Pending creation now opens one authoritative IndexedDB transaction across memberships, expenses, settlements, and audits; it reconstructs the current ledger, reruns the Phase 3 balance/recommendation/pair policy, and commits the Pending snapshot plus audit only when the requested recommendation is still exact. Native and fake IndexedDB state-drift tests prove stale creation rolls back.

**Verification:** focused Phase 8 domain/application/persistence/component suites passed; full Vitest passed 309 tests across 36 files; architecture guards passed 11 cases; lint and TypeScript passed; production build passed with `/settlements` statically prerendered; full Playwright Chromium passed 27 flows, including six Phase 8 flows for cross-identity Mark Paid/Confirm, Reject, sender Cancel, duplicate Pending, reversed stale original-amount confirmation, close/reopen persistence, responsive breakpoints, keyboard focus return, and zero serious/critical Axe findings. Dependency audit found zero vulnerabilities. Manual audits found no settlement mathematics in React, no alternate recommendation engine, no persisted balances/recommendations/badge counts, no Phase 9 functionality, and no new floating-point financial arithmetic. `git diff --check` passed.

### Phase 9 - Cards

Build private card CRUD, palette/type UI, no-card path from expenses, archive behavior, and owner-only projections. **Reasoning: medium-high** because UI is contained but privacy is critical. Exit: cross-identity tests prove metadata is absent - not merely hidden - and referenced history remains valid.

**Plan status (2026-08-18): approved and authorized.** Exact scope, exclusions, component hierarchy, owner-scoped application projections/use cases, stable palette identifiers and migration, create/edit/remove workflows, historical Expense snapshot rules, household-independent privacy model, atomic Card/Expense persistence changes, responsive/accessibility behavior, proposed files, test matrix, risks, and recommended model/reasoning are documented in `PHASE_9_PLAN.md`. Approved clarifications keep Card lifecycle data out of broad Household audits, map only known legacy color tokens while failing unknown values through typed atomic migration errors, require refreshed consent when Delete becomes Archive, and require committed Expense snapshots to derive from authoritative owner-scoped Card state. Implement Phase 9 only and leave it uncommitted for review.

**Result: complete and accepted (2026-08-19; committed as `d96451e`).** Replaced the Cards placeholder with household-independent owner-private Card management for trimmed duplicate-capable names, Debit/Credit type, and the six stable palette identifiers. Added accessible create/edit/menu/remove dialogs; authoritative physical-delete versus archive behavior with stale-consent rejection; owner-keyed identity reconstruction; active-only Expense selection; retained archived associations; historical owner-private snapshots; and transaction-time Card ownership/activity checks that derive new Expense snapshots from authoritative Card state rather than React metadata. IndexedDB v3 atomically migrates `lime`, `blue`, and `gray`, preserves approved identifiers, and rejects unknown palette values through a non-sensitive typed failure without partial upgrade. Card lifecycle events remain outside broad Household audit data. No Phase 10, Appwrite, banking, card-number, or payment-processing scope was added.

**Verification:** focused Card validation/projection/application/persistence/component suites passed, including all palette values, duplicate names, no-Household ownership, foreign-ID anti-enumeration, historical/future snapshots, archived preserve/switch, stale Delete consent, and atomic migration rollback; full Vitest passed 323 tests across 39 files; architecture guards passed 11 cases; lint and TypeScript passed; production build passed with `/cards` statically prerendered; full Playwright Chromium passed 31 flows, including four Phase 9 flows for create/edit/delete/reload, cross-identity isolation, pending/no-Household use, referenced archive/history, keyboard/mobile behavior, and zero serious/critical Axe findings. Dependency audit found zero vulnerabilities. Manual privacy/source audits found no presentation repository/IndexedDB imports, no Household-wide Card query, no broad Card audit writes, no raw private metadata submitted by Expense React, and no Phase 10 functionality. `git diff --check` passed. The optional `agent-browser` launcher failed its own sandbox launch diagnostic (`CDP response channel closed`), while project Playwright Chromium completed all browser checks successfully.

### Phase 10 - Household management and leadership

Build member/request management, role transfer, leave/remove/delete gates, explanatory disabled states, and historical-member protections. **Reasoning: high** due permissions and irreversible-looking actions. Exit: role and financial-gate matrix passes with explicit confirmations and retained history.

**Plan status (2026-08-19): approved and authorized.** Exact scope, exclusions, page/component hierarchy, presentation-safe models, Phase 6 Join Request reuse, Leave/Remove/Transfer/Delete workflows, transaction-time financial and authority revalidation, former-member/history/private-Card behavior, responsive/accessibility requirements, proposed files, comprehensive test matrix, risks, and recommended model/reasoning are documented in `PHASE_10_PLAN.md`. Approved clarifications freeze `household-closed` as the deletion-only terminal status for Pending join requests, last-role retention for former memberships without authority, no Household rename UI or former-member portal, one explicit deletion confirmation, and full atomic deletion/rollback semantics. Implement Phase 10 only and leave it uncommitted for review.

**Result: complete and accepted (2026-08-19; committed as `c3585fe`).** Replaced the active Household placeholder with role-shaped identity, masked exact House Code controls, Leader summary, deterministic active-member list, Leader-only Pending-request review, Leave, Remove, Transfer Leadership, and a separate Household deletion danger area. Added exact current-balance and bidirectional Pending-settlement previews, while named IndexedDB transactions reread the active Household, complete membership history, Expenses, Settlements, Join Requests, and current authority before mutation. Leave/Remove/Delete release conditional active-membership keys while retaining former membership and last role; only active Leader role authorizes. Deletion tombstones the Household, retains the reserved code, converts active memberships to former, closes only Pending Join Requests as `household-closed`, appends sanitized audits, and never includes Cards, private snapshots, receipts, or unrelated history in its write set. Existing V1 requests remain readable without a database-version bump, and ordinary transition APIs reject `household-closed` at both type and runtime boundaries. No rename UI, former-member portal, Phase 11, or Appwrite work was introduced.

**Verification:** focused domain, projection, service, persistence, and component tests cover directional financial blockers, terminal Settlement non-blocking, last-role retention without authority, exactly-one-active-Leader transfer, commit-time Accept/Reject authority, active-key release, deletion gates, Pending-request closure, stale post-deletion request creation, terminal-request preservation, V1/V2 reconstruction, injected transaction rollback, and byte-for-byte preservation of financial/private stores. Full Vitest passed 346 tests across 41 files; architecture guards passed 11 cases; lint and TypeScript passed; the production build passed with `/household` statically prerendered; full Playwright Chromium passed 37 flows, including six Phase 10 management/responsive journeys, native IndexedDB reopen, cross-identity transfer/leave/remove/delete, exact leading-zero clipboard copy, route revocation, keyboard focus behavior, no horizontal overflow, and zero serious/critical Axe findings. The dependency audit found zero vulnerabilities. Manual source/privacy audits found no presentation persistence imports, no Household Card projection, no deletion access to Card/receipt stores, no raw audit projection, no rename UI, and no Phase 11 implementation. `git diff --check` passed.

### Phase 11 - Dashboard, analytics, and monthly reports

Build selected-month spending modules, current-state outstanding/health/balances, dynamic daily Recharts bar graph, payment mix, recent expenses, allowed analytics, and Dashboard-linked report. **Reasoning: medium-high** due date aggregation and distinction between monthly and current state. Exit: leap-year/month-boundary aggregation tests, accessible chart summaries, and responsive visual QA.

**Result: complete and approved at the pre-Appwrite checkpoint (2026-08-22).** Replaced the Dashboard placeholder with the canonical heading-free top controls, active-member initials, selected-month Spent/daily bars/amount-based Payment Mix/Recent Expenses, and current-state Outstanding/Settlement Health/Housemate Balances. Added `/reports/monthly?month=YYYY-MM` with safe current-local fallback, exact Total/Count/month comparison, Paid versus Share including former members, largest Expenses, viewer-local Settlement activity, and a separately labelled current position. Pure application analytics use date-only Expense Date filtering, BigInt ratio arithmetic, privacy-safe projections, existing balances/recommendations, Pending-pair suppression, and no aggregate persistence. Recharts is limited to the two client chart components; the existing Expenses route now consumes the selected-month deep link.

**Verification:** full Vitest passed 368 tests across 45 files; architecture guards cover layer boundaries, the sole Recharts import boundary, absence of persisted analytics aggregates, and no timezone conversion in Expense month aggregation. Lint passed with zero warnings; TypeScript passed; production build passed and statically prerendered `/dashboard` and `/reports/monthly`; full Playwright Chromium passed 43 journeys, including six new analytics/report flows, exact selected/current-state separation, leap-year February, mutation and identity reconstruction, keyboard month controls, reduced motion, mobile/tablet/desktop no-overflow checks, no private Card metadata, and zero serious/critical Axe findings. `npm audit` reported zero vulnerabilities; `git diff --check`, exact-finance, privacy, date-only/timezone, scope, and visual Dashboard/report reviews passed. The separate `agent-browser` visual CLI could not establish its CDP channel after two attempts, so visual/error verification was completed through Playwright Chromium and full-page screenshots instead; no page, hydration, overlay, or console errors were found.

**Follow-up presentation correction (2026-08-22):** Reflowed Dashboard analytics so Spending Trend spans the full content grid and Payment Mix shares a responsive supporting grid with Housemate Balances and Recent Expenses. Kept the existing Recharts dataset, exact summaries, payment values, chart semantics, and all financial/current-state behavior unchanged. The chart now fits every day label from 390px upward with compact mobile typography; only a contained chart-only fallback remains below that narrow boundary. Added responsive geometry, endpoint, tick-overlap, month-length, accessibility, and no-page-overflow coverage.

### Receipt Retention Foundation - provider-independent/local

**Result: complete and approved at the pre-Appwrite checkpoint (2026-08-22).** Added validated V2 receipt metadata with canonical `available`, `user-deleted`, and `retention-expired` terminal lifecycle rules; atomically migrated IndexedDB v3 to v4 with strict rejection of missing/invalid creation timestamps and contradictory legacy deletion state; added the retention-oriented compound index; implemented the provider-independent `Asia/Dhaka` three-calendar-month cutoff and privileged bounded/idempotent retention service; migrated manual deletion to explicit user-attributed state; and made Add/Edit/Details status-aware without Blob reads or binary actions for terminal metadata. The local runtime, React context, development tools, and normal user actions expose no retention runner. No Appwrite, scheduler, cron, financial compaction, or unrelated feature was introduced.

**Verification:** cutoff/lifecycle suites cover January and year rollover, February, leap-year February, March, August, exact cutoff inclusion, one-instant-before expiration, and Expense Date independence. Migration tests cover V1 available reconstruction, legacy tombstone reconstruction, v3→v4 success/index creation, strict malformed rollback, and no timestamp guessing. Service/repository tests cover delete-first ordering, Storage-style failure, metadata failure recovery, already-missing content, deterministic bounded pages, repeated execution, terminal skips, and manual-deletion races. Raw before/after records and application projections prove Expenses, allocations/basis points, balances, recommendations, Settlements, Dashboard, Monthly Reports, and Leave/Remove/Delete Household gates remain identical; soft-deleted Expense and tombstoned Household receipts remain eligible. Full Vitest passed 429 tests across 56 files; architecture guards passed 15 cases; lint and TypeScript passed; the production build passed; Playwright passed 65/65 across Chromium, Firefox, and WebKit, including available/expired/user-deleted Receipt UI with zero serious/critical Axe findings. The dependency audit found zero vulnerabilities; browser visual/error inspection passed; `git diff --check` and manual financial/privacy/date/scope audits passed.

Explicit exclusions: Appwrite SDKs/adapters, Storage deletion, scheduled Functions, cron configuration, browser-open cleanup, public purge controls, financial checkpointing/compaction, and unrelated product work.

### Corrective phase - Confirmed Settlement Financial Lock

**Result: complete and approved at the pre-Appwrite checkpoint (2026-08-22).** This corrective phase intentionally supersedes the previous allowance for old Expense financial edits to recalculate balances after a Confirmed Settlement. It derives the latest same-Household Confirmed `resolvedAt` boundary and locks an Expense exactly when `expense.createdAt <= latestConfirmedSettlementAt`; Expense Date and non-Confirmed statuses never contribute.

The implementation adds an independently composed `confirmed-settlement` editability reason and `EXPENSE_FINANCIAL_HISTORY_LOCKED`; a complete financial fingerprint including percentage basis points, deleted state, and opaque private Card-association identity; immutable `expenseId`, `householdId`, `creatorId`, and `createdAt`; transaction-time Expense/membership/Settlement/private-Card rereads; atomic stale Save/Delete rollback; a single policy-enforced product mutation path without raw Expense `create`/`replace`/`markDeleted` repository methods; and privacy-safe Edit/Details/Delete states that keep authorized Expense Name and receipt lifecycle editing available. The boundary is derived rather than persisted, so no IndexedDB migration is required. No historical reconstruction, adjustment/correction record, Settlement reversal/reopening, Appwrite, or unrelated feature is included.

**Verification:** focused domain/application/persistence/component/browser coverage passes the approved matrix for before/equal/after timestamps, Pending/Rejected/Cancelled/Confirmed statuses, multiple boundaries, backdated post-confirmation Expenses, Creator/Leader enforcement, every financial fingerprint property including Card transitions, Name/receipt-only edits, immutable replacement identity/history, composed former/legacy causes, stale Save/Delete races, and complete rollback of Expense/receipt/Card-snapshot/audit/Settlement changes with unchanged balances and recommendations. Full Vitest passed 446 tests across 57 files; architecture guards passed 16 cases; zero-warning lint, TypeScript, optimized production build, `npm audit`, and `git diff --check` passed. Full Playwright passed 68/68 across Chromium, Firefox, and WebKit, including the new real-IndexedDB stale Save/Delete flows and zero serious/critical Axe findings. Manual source audits confirmed no Expense Date boundary use, no private Card leakage in leader projections, exact integer-poisha financial sources unchanged, and no Appwrite dependency/import or implementation.

### Phase 12 - Local MVP stabilization

Complete responsive behavior, accessibility audit, empty/loading/error/success states, full Playwright journeys, visual regression/gut checks, performance checks, dependency/cost audit, and defect fixes. No new scope. **Reasoning: high** for cross-feature debugging and financial regression review. Exit: agreed local-MVP acceptance checklist green and user declares local MVP stable.

**Checkpoint result (2026-08-22): exit satisfied by the approved cumulative stabilization, UI-correction, retention, financial-lock, and H1-H8 hardening work.** The final release-style matrix is green across full Vitest, architecture guards, lint, typecheck, production build, Chromium/Firefox/WebKit Playwright, responsive and Axe checks, dependency audit, financial/privacy/date audits, and Git diff validation. The user approved the complete local application for the `pre-appwrite-local-v1` handoff checkpoint. Appwrite remains unimplemented.

**Re-hardening result (2026-08-23): Phase 12 re-executed over all post-checkpoint drift at the owner's request to produce a fresh Local MVP Freeze candidate.** The working tree since `d678e88` (rename feature, Card Designs, Member Contributions, bootstrap retention sweep, optimization pass, UI parity corrections, Dashboard UX polish) was re-audited across the twelve scope areas. One serious finding was fixed: metric enter animations used opacity fades that fail WCAG contrast while running and broke the stabilization-matrix Axe gate — motion is now transform-only (`slide-in-from-*`, no `fade-in-*`). Two touch-target gaps were hardened (month steppers 40→44px beside the 44px combobox; mobile-only section-collapse buttons 32→40px). Audits confirmed existing coverage elsewhere: atomic IndexedDB writes/OCC/idempotent protected creates with rollback tests (persistence), 15 double-submit guard sites (`disabled={saving}`/`aria-busy`) across forms and dialogs, zero web-storage state leakage, privacy projections covered by cross-identity e2e assertions, and responsive geometry asserted from 360–1440px.

**Verification (2026-08-23 freeze matrix):** full Vitest green at 486/486 across 61 files including architecture guards; lint and TypeScript clean; production build green with the expected static/dynamic route split (12 static, 2 dynamic expense routes); `npm audit` zero vulnerabilities; `git diff --check` clean; Playwright Chromium 69/69 after fixes; Firefox/WebKit smoke 2/2; focused analytics/stabilization/responsive suites 20/20 post-change. The Local MVP Freeze is recommended subject to owner approval; Phase 13 remains gated.

**FREEZE APPROVED (2026-08-23):** the owner approved this verified state as the authoritative pre-Appwrite Local MVP baseline. Product behavior — business requirements, financial calculations, expense/settlement rules, confirmed-settlement financial lock, Expense OCC, backdated/future-date protections, receipt privacy/retention/quotas, idempotency semantics, household management, Card privacy, Dashboard/Reports, UI/UX, responsive behavior, and accessibility behavior — is frozen from this checkpoint onward; Phase 13 adapts the provider without casually rewriting these rules. Deferred to Phase 13 by owner decision: trusted server time, signed confirmations, real cross-device OCC, Appwrite Storage quota/reservations, scheduled receipt retention, production authentication/session enforcement, anti-enumeration/rate limiting, and real server permissions.

### UI Parity Pass - Canonical Figma alignment

**Result: complete and approved at the pre-Appwrite checkpoint (2026-08-22).** Applied UI-P1 through UI-P7 sequentially as a presentation-only pass. The shell now matches the 232px canonical sidebar/account treatment; Dashboard, Expenses, Add/Edit Expense, and Expense Details reproduce the approved 1440 x 1024 Figma geometry with responsive Grid/Flex composition; non-canonical screens inherit the same compact white-surface, 20px-radius, neutral-border, black-action, and lime-accent grammar. Presentation pagination remains downstream of application filtering/sorting, private Card detail remains projection-gated, invalid receipt previews use a non-mutating fallback, and the development identity control is isolated from product/authentication navigation. No domain, application-service, repository, persistence, permission, privacy, route, or financial behavior was changed by the parity work.

**Verification:** direct full-page screenshot and geometry inspection passed for the four canonical screens at 1440 x 1024. Responsive inspection passed at 1280, 1024, 768, 430, 390, and 360 pixels without horizontal overflow. Full Vitest passed 368 tests across 45 files; architecture guards passed 14 cases; lint, TypeScript, optimized production build, and git diff --check passed; full Playwright Chromium passed all 43 journeys with zero serious/critical Axe findings and no page, console, hydration, or error-overlay failures; the production dependency audit reported zero vulnerabilities. Exact-money, Card privacy, Household authority, historical-member, date-only/timezone, and analytics reconstruction behavior remained green through the unchanged regression suites.

### Follow-up presentation correction - desktop sidebar collapse

**Result: complete and approved at the pre-Appwrite checkpoint (2026-08-22).** Added a controlled desktop sidebar collapse/expand interaction that smoothly transitions the shell to an icon rail, keeps route/navigation/account/development controls available with accessible labels and titles, and reveals the collapsed expand control in place over the logo on hover/focus without protruding into dashboard content. The mobile/tablet bottom navigation is now icon-only with centered controls while retaining accessible names, hover titles, routing, and More-sheet behavior. Collapse state is presentation-only and is not persisted. No domain, application-service, repository, persistence, permission, privacy, or financial behavior changed.

**Verification:** focused shell tests and the responsive shell Playwright suite passed, including the collapsed-logo hover journey and icon-only mobile controls. The final checkpoint gates supersede the earlier temporary dirty-worktree fixture issue: full Vitest, typecheck, lint, build, architecture, and the complete multi-browser Playwright suite are green.

### Follow-up presentation corrections - attention badges, filter sizing, hydration resilience

**Result (2026-08-22).** Restored the frozen leader join-request attention badge: the desktop Household item now shows the pending-review count pill like Settlements, and mobile surfaces it on the More trigger and the Household row inside the More sheet, all derived from the existing household access projection with contextual accessible names. Converted the `>=1400px` Expenses filter grid from fixed pixel tracks to content-driven `minmax()` tracks with an intrinsic Clear Filters button, eliminating label truncation and oversized controls. Added scoped `suppressHydrationWarning` to the fixed mobile navigation bar and root `<body>` because force-loaded browser extensions on this machine inject pre-hydration DOM attributes (`style="translate: ..."`, `cz-shortcut-listen`) that React cannot reconcile; child checking is unaffected. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.

**Verification:** lint and typecheck clean; full Vitest green at 473/473 across 60 files (four new shell badge tests); focused Chromium Playwright suites for responsive shell, expenses, select controls, settlements, household onboarding, and accessibility all passed; an additional seeded end-to-end check proved both badges render from real IndexedDB data and measured zero truncation/overflow across all filter controls at 1440px before the temporary spec was removed.

### Follow-up presentation correction - expense form panel sizing

**Result (2026-08-22).** Removed the `>=1400px` fixed pixel heights on the Add/Edit Expense details, receipts, and split panels. The panels now size from content, so the Payment Method area collapses for Cash and expands for Card selection, and receipt upload content stays inside its section instead of overflowing past the surface border. Canonical column tracks, spacing, and paddings are unchanged. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.

**Verification:** geometric Chromium check at 1440x1024 proved zero descendant overflow in Cash and Card states with dynamic panel growth (details 220px to 306px), full Vitest stayed green at 473/473, lint and typecheck passed, and the complete expenses Playwright suite passed 19/19 before the temporary verification spec was removed.

### Follow-up presentation correction - sidebar collapse control styling

**Result (2026-08-22).** Restyled both desktop sidebar toggle states as restrained ghost icon buttons: transparent background, muted icon color with a subtle secondary hover square, 12px radius, and no shadow. The collapsed expand reveal keeps its approved hover/focus-over-the-logo behavior but now crossfades opacity only inside the logo's exact 40px rectangle instead of scaling a solid dark pill over it, eliminating the circular appearance (the previous `rounded-xl` token resolves to a 20px radius) and the perceived upward drift. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.

**Verification:** a temporary Chromium check measured the revealed control at exactly 40x40 with zero top/left offset versus the logo, matching 12px corner radius on both rectangles, transparent background, no shadow, and no transform; full Vitest stayed green at 473/473, lint and typecheck passed, and the responsive shell Playwright suite including the collapsed-logo hover journey passed before the spec was removed.

### Follow-up feature - Dashboard Member Contributions

**Result (2026-08-22; owner-approved requirement addition recorded in `REQUIREMENTS.md`).** Added a `Member Contributions` card to the Dashboard summary row. The application projection reuses the existing Monthly Report contributions engine (`calculateMemberContributions`) over non-deleted selected-month Expenses, projects presentation-safe member identities including former-member labeling, orders deterministically by paid amount descending then display name then member ID, and enforces the same reconciliation invariant that member payments sum exactly to Spent. The card renders exact tabular BDT amounts per payer with an empty state for months without spending and contained scrolling beyond three rows. The `>=1400px` summary grid now uses four flexible `minmax()` tracks (replacing fixed pixels) with `min-width: 0` guards; below 1400px it reflows to two columns and mobile stacks. No persisted aggregates, balance rules, recommendation logic, or privacy behavior changed.

**Verification:** new unit tests cover ordering, former-member inclusion, deleted-expense exclusion, and empty months; component tests assert exact rendered amounts and empty state; full Vitest green at 476/476 across 61 files; lint and TypeScript passed; analytics Chromium e2e passed 9/9 including responsive and Axe checks; a seeded browser check measured all four cards on one row at 1440px with zero overflow and correct seeded totals before removal.

### Follow-up presentation refinement - Dashboard UX polish

**Result (2026-08-23; owner-requested from a UI/UX review; scope in `UI_UX_POLISH_PLAN.md`).** Presentation-only refinements across the Dashboard, month selector, charts, and development tooling. Month switching now renders the previous view dimmed with `aria-busy` and an inline updating indicator instead of replacing the grid (stale-while-revalidate); fixed heights became floors (`min-h-36` summary cards, `min-h-[366px]` Recent Expenses, `min-height:` for all `>=1400px` expense panels) so content can grow without clipping while chart canvases stay bounded; 24 arbitrary text-size utilities were consolidated into font-size-only theme tokens (`text-mini/text-fine/text-row/text-stat`); empty states gained an icon-and-CTA treatment linking to `/expenses/new`; Housemate Balances, Recent Expenses, and Payment Mix gained mobile-only collapse toggles (default expanded, desktop unaffected); `nextCalendarMonth` was added beside `previousCalendarMonth` and MonthSelector grew clamped Previous/Next stepper buttons; daily bars distinguish weekends and mark today during the current month (replacing the unconditional last-day highlight) while non-compact Payment Mix slices show rounded percentage labels beside exact textual rows; Spent and Outstanding money values re-mount keyed by value with reduced-motion-safe fade/slide; and the desktop sidebar's dev-tools strip is hidden whenever the composition root omits development identities, keeping environment branching exclusively in the composition root per the architecture guard. No domain, application-service, repository, persistence, permission, privacy, or financial behavior changed.

**Verification:** full Vitest green at 486/486 across 61 files including new coverage for stepper emission/clamping, empty-state CTAs, collapse semantics, weekend derivation, and the tools gate; lint and TypeScript clean; analytics Chromium e2e passed 10/10 with responsive geometry and zero serious/critical Axe findings.

**Amendment (2026-08-23, owner request):** the Member Contributions empty state no longer offers the Add Expense CTA — it keeps only the icon and message; Recent Expenses retains its CTA. Coverage asserts the single remaining dashboard "Add Expense" link and the absence of links in the contributions section. Vitest 486/486, lint/typecheck clean.

**Amendment (2026-08-23, owner-reported layout defects):** fixed the Member Contributions card that had been missed by the height-floor conversion (`h-36` straggler caused its grey empty box to overflow the card border while sibling cards stretched) — card is now `flex min-h-36 flex-col` and the empty box fills the remaining height as a centered horizontal row whose bottom edge sits on the padding line; the Recent Expenses empty message and Add Expense action now center in both axes inside their panel. Vitest 486/486, lint/typecheck clean.

### Follow-up feature - Card Designs

**Result (2026-08-22; owner-approved requirement change recorded in `REQUIREMENTS.md`).** Replaced the Card color-swatch picker with six realistic payment-card designs (Red, Yellow, Black, Blue, Green, Orange). A shared preview component renders the entered Card name, owner display name, Debit/Credit badge, and decorative demo number/expiry constants that never enter form state or persistence. The design picker keeps Radix radio semantics through a transparent input covering each tile (the project's canonical custom-control pattern), and My Cards tiles render the chosen design full size with the actions menu overlay. Storage widened additively: six new stable identifiers joined `CARD_COLOR_IDS`, every legacy identifier stays valid for reading persisted Cards and private historical snapshots without rewriting them, no schema-version bump was required, and V1 legacy color migration now anchors to `LEGACY_CARD_COLOR_IDS` so its determinism cannot drift when vocabulary grows.

**Verification:** full Vitest green at 476/476 across 61 files including schema coverage of the widened vocabulary, a design-id create/persist/read round trip with ownership isolation, live-preview and demo-value isolation assertions in component tests, and unchanged legacy migration expectations; lint, TypeScript, and architecture guards passed; cards Chromium e2e passed 4/4 including cross-identity privacy, historical-snapshot immutability after redesign, mobile usability, and zero serious/critical Axe findings on the new previews.

### Phase 13 - Appwrite architecture and integration

**Plan status (2026-08-29): Phases 13A/13B complete (`bed70e3`); R1 `5fed47f`; R2 `9ecd4d3`; R3 `01d82897e246aadee611d01ca977b1fb0cb3d1d4`; R4 `840d907211f5d79793096613539981c099049161`. Live Schema V4 remains clean and sufficient. R5 Appwrite Sites production-readiness implementation, feature-branch deployment, and full QA are authorized in `R5_PLAN.md`; merge/release/tag remain unauthorized.**

#### R2 SIMPLIFICATION - Owner Decision (2026-08-26)

**Owner-directed simplification of R2 execution gates:**

1. **Gate C second-project requirement REMOVED** — No disposable Appwrite project required. No `.env.gate-c.local` required. No second-project restore drill required. Backup/restore tooling remains as operational tooling (not a gate).

2. **Existing Appwrite project is authoritative** — Continue using `APPWRITE_ENDPOINT`, `APPWRITE_PROJECT_ID`, `APPWRITE_RUNTIME_API_KEY`, `APPWRITE_BOOTSTRAP_API_KEY`. No synchronization, staging/production replication, or multi-environment infrastructure.

3. **Current project safe for controlled testing** — Business state is effectively empty (sanitized counts verified: households=0, memberships=0, join_requests=0, expenses=0, settlements=0, cards=0, receipt_metadata=0). Controlled R2 testing authorized.

4. **Backup tooling kept, gate simplified** — Before destructive tests: run backup, verify manifest/checksums, store outside repo via `APPWRITE_BACKUP_DIR`. R2 backup = database rows only (receipt binary backup = R4). No full backup → destroy → restore drill required.

5. **Proceed with real R2 command plane** — Finish ten Household commands using trusted actor, server Clock, Appwrite transactions, coordination guards, OCC, idempotency, four-member limit, frozen financial gates. No business rule weakening.

6. **Wire existing Household UI** — Replace R1 disabled actions with real R2 endpoints as verified. No UI redesign. Local IndexedDB mode unchanged.

7. **Real-project acceptance journey** — Primary: existing user → Create Household → persist → reload → verify. Secondary (if second user ready): Join → Pending → Accept → Member → reload both → verify roles.

8. **Test delete using temporary/empty Household only** — Create controlled Household (balances=0, pending settlements=0), backup+verify, exercise deleteHousehold, verify tombstone/former/closed/audit/idempotency. No destructive deletion after meaningful financial data exists.

9. **Restore tooling does not block R2** — backup/verify/restore fully tested with unit/integration/stub coverage. Live restore drill deferred.

10. **Enable Household capabilities** — After each command path green, enable corresponding capability. By R2 completion, Household feature genuinely functional in Appwrite mode.

11. **Final R2 verification** — command-kernel, parity, adversarial, full Vitest, architecture guards, ESLint, TypeScript, production build, dependency audit, git diff --check, secret/client-bundle audit, Chromium, Firefox/WebKit smoke, Axe. Then real-project Household journey.

12. **Update release plan** — Record: separate project NOT REQUIRED, existing project = single production backend, backup verified before controlled destructive test, live restore drill = deferred operational procedure.

13. **STOP BOUNDARY** — Complete R2 only. Do not start R3 (Expense production mutations, Settlement production mutations, Card production mutations, Receipt production mutations). Leave R2 uncommitted for final review.

#### R1 execution result - production read data plane (2026-08-26): IMPLEMENTED AND VERIFIED, UNCOMMITTED

Owner-authorized R1 executed on `feature/phase-13-appwrite`. The 13B checkpoint was verified as already committed (`bed70e3`). Delivered: finalized strict provider row mappers (`src/infrastructure/appwrite/reads/mappers.server.ts`) with fail-closed validation incl. BigInt-poisha safe-integer boundaries and lifecycle contradictions; read-only repositories over a bounded-paging TablesDB reader; trusted request context resolving the actor exclusively from the HttpOnly session cookie with idempotent Profile bootstrap and allowlist re-check; a 17-endpoint sanitized same-origin read surface under `/api/app/*` (no-store, typed error mapping, house-code lookup/generation throttled 10/h); the typed `ProductCapabilities` model disabling every write family with restrained notices; server-authoritative Asia/Dhaka business date in production only; composition swap to `ProductionApplicationRuntime -> ApplicationRuntimeProvider -> AppShell -> HouseholdAccessGate -> routes` with the milestone screen deleted; composition-aware working Log Out (production) preserving the local explained placeholder. Additive application-layer refinements: `MemberIdentityView` privacy projection for batch profile lookups (other members never carry contact data), `ApplicationValues`/`Dependencies` exports, `RATE_LIMITED`/`INVALID_INPUT`/`COMMANDS_UNAVAILABLE` codes, BigInt-safe JSON transport helpers. Architecture guards amended and extended: `/api/app/**` approved boundary, production providers free of IndexedDB/dev-tooling/server modules, adapter modules server-only. New coverage: 12 mapper tests, 8 repository tests (owner gating, paging, tombstone hiding, write-guard rejection), 6 runtime-context/bootstrap tests incl. Dhaka date boundary, provider-parity suite proving Appwrite-read projections deep-equal seeded local projections across dashboard/report/settlements/expenses/cards/receipts (BigInt basis points included), transport round-trip, capability-gating component tests (production disabled vs local enabled), production provider state machine (shell render, anonymous redirect, gate authority, unavailable retry). Live verification against the real project: anonymous acceptance 3/3 in Chromium, Firefox, and WebKit (login redirect, Axe serious/critical zero, registration absent, no finance IndexedDB), endpoint probes 401-sanitized, live business-table counts `{profiles:1, all business tables:0, coordination_guards:44 throttle rows}` proving zero fake data. Gates: typecheck clean; ESLint zero warnings/errors; production build green with the full `/api/app` route table; `npm audit` zero vulnerabilities; `git diff --check` clean; full Vitest 594/594 across 78 files under `--fileParallelism=false` (default parallel mode remains intermittently timeout-flaky on this machine under background harness load — pre-existing, evidenced before R1 files landed; the 10 MiB receipt-boundary test received a scoped 20 s timeout). Known R2 inputs discovered: four additive schema columns required by future writers (listed in PROJECT_STATE). First-login proof remains deferred pending owner-entered credentials via the updated opt-in `auth-live` spec. Frozen envelope: 1 Household, max 4 active members, max 4 approved production accounts, ~15 Expenses/month. `ALLOWED_ACCOUNT_EMAILS` remains the current fail-closed approved-account list. Production accounts remain admin pre-provisioned and registration remains locked down. The owner now prefers eventual self-registration, but implementation is an unresolved dedicated follow-up because account ownership without email verification has not been designed; do not restore `/api/auth/register` or add verification implicitly. Appwrite Auth email is authoritative and password recovery establishes or changes password ownership. Trusted Next.js command boundaries use only the runtime credential; schema bootstrap and temporary account provisioning remain strictly separate.

Only after the stability gate: design collections/indexes/permissions, production auth, private receipt storage, atomic operations, anti-enumeration controls, Appwrite repository adapters, and the once-daily privileged receipt-retention Function. The Function must derive eligibility from validated metadata, calculate the real `Asia/Dhaka` cutoff at execution time, remove content before conditionally marking metadata, recover from already-missing files and partial metadata failures, use bounded deterministic batches, and remain unavailable to normal clients. Exact scheduler timezone/cron configuration must be verified during Phase 13 rather than assumed earlier. Preserve application/domain APIs. **Reasoning: high** due security, migration, and concurrency. Exit: local and Appwrite adapters pass shared contracts; server-side authorization and idempotent scheduled retention are verified.

#### Slice 13A - Foundation + reproducible schema

**Scope:** server SDK dependency; `src/infrastructure/appwrite/` config (fail-closed approved-account configuration, runtime/bootstrap credential separation), guard/outcome ID derivation, declarative schema definitions (14 tables incl. `schema_metadata`), private bucket definition, maintenance Function skeleton, bootstrap planner/applier/CLI; architecture containment guards; env example. **Exclusions:** all auth flows, repository behavior, product commands, retention execution, UI changes, remote cloud operations.

**Result (2026-08-23): complete and committed.** Added `node-appwrite@28` server dependency and `tsx` dev tooling; config module with fail-closed approved-account configuration (missing/blank/malformed/>4 normalized unique entries disables the approved set; duplicates collapse before the four-seat cap) and separated runtime/bootstrap credentials; cryptographic guard/outcome row-ID derivation within the provider's 36-character constraints; declarative definitions matching every approved correction (BigInt money columns only; zero Card fields on `expenses`; separate `expense_card_private_details`; no lock state; guard/reservation/metadata infrastructure); read-diff bootstrap planner that never plans destructive operations and reports unmanaged columns as drift-only; dry-run-first applier refusing remote mutation without the bootstrap key and explicit `--yes`; CLI entry points `npm run appwrite:plan|appwrite:apply`; architecture guards confining the SDK and secrets.

**Verification:** full Vitest green at 517/517 across 65 files (31 new tests: allowlist fail-closed matrix, ID determinism/constraints, schema conformance incl. forbidden-field scans, planner create/drift behavior, applier ordering/dry-run safety, containment guards); lint, TypeScript, production build, `npm audit`, and `git diff --check` clean. No cloud resources were touched: apply requires explicitly supplied `APPWRITE_BOOTSTRAP_API_KEY` plus endpoint/project ID, prints the exact resource list, and is destructive nowhere (creates/upserts only).

**Slice 13A-2 - Cloud bootstrap apply (2026-08-23): COMPLETE against the live project.** First real apply surfaced Appwrite's asynchronous column-build window (`memberships.by_household_status` rejected while the enum column was still provisioning). Owner-approved correction implemented explicit readiness barriers instead of blind retries: after each table's columns are created, the applier polls provider status until every required column reports `available` (processing = wait; failed/stuck/deleting = immediate fatal stop with sanitized report); indexes get the identical barrier; a narrow fallback re-reads column availability and retries only when the provider returns the specific transient column-not-available error; bounded at ~2 s interval / ~60 s barrier with `APPWRITE_RESOURCE_PROVISIONING_TIMEOUT`; planner classifies existing resources as ALREADY CORRECT / PROVISIONING / DRIFT from live statuses and schema_metadata stays unwritten until everything is confirmed available. Resumed apply completed 123 operations from the accepted partial state (no deletes/drops/duplicates), then post-apply plan reported CREATE 0 / DRIFT 0 / PROVISIONING 0 with all 14 tables complete. Live property verification confirmed three BigInt money columns; zero Card/lock fields on `expenses` with `expense_card_private_details` separate; `households.code_unique` and guard `logical_key_unique` unique indexes available; receipts bucket private (File Security on, no granted permissions, 10 MiB, jpg/jpeg/png/webp, encrypted); maintenance Function schedule `0 0 * * *`, timeout 300 s, empty client execution, six minimal scopes; `schema_metadata.active.version = 1`; every business table contains zero rows. Full Vitest 528/528 across 66 files (9 new readiness/resume tests on stubbed SDK responses); architecture guards, lint, TypeScript, production build, dependency audit, diff-check clean. Provisioning-resilience source changes and this evidence are left uncommitted for owner review; 13B remains unauthorized.

**Slice 13B - Authentication + Production Session (2026-08-25): IMPLEMENTED on `feature/phase-13-appwrite`, uncommitted pending owner review and final verification.** Server-only Appwrite auth clients use the runtime API key only for normal Account session creation plus table/profile operations; authenticated user reads and `deleteSession("current")` use the per-request session client. The HttpOnly `hft_session` cookie contains only the Appwrite session secret. Production composition gates product routes behind the authenticated milestone and initializes no local IndexedDB financial runtime or DEV identity tooling.

**Frozen production identity model:** at most four admin-pre-provisioned accounts; `ALLOWED_ACCOUNT_EMAILS` is the sole authoritative approved-account list and fails closed when missing, empty, malformed, or over four normalized unique emails. Self-service registration, email verification, authenticated-unverified state, and production email editing are unsupported. `/register` is informational and `/api/auth/register` does not exist. Appwrite Auth email is authoritative. Password recovery establishes first ownership and remains the password-change path; Profile bootstrap occurs idempotently on successful login.

**Credential isolation:** `APPWRITE_RUNTIME_API_KEY` remains normal runtime-only with `sessions.write`, `tables.read`, `rows.read`, and `rows.write`; `APPWRITE_BOOTSTRAP_API_KEY` remains schema tooling-only; temporary `APPWRITE_PROVISIONING_API_KEY` is loaded only by the admin provisioning CLI configuration and requires `users.read`/`users.write`. Provisioning generates a bootstrap password that is never printed, returned, or application-stored. The existing real account is treated as already provisioned. No additional real account may be provisioned until explicit owner authorization.

**Abuse protection:** HMAC-opaque fixed-window throttles apply to login (10 per 15 minutes), recovery (3 per hour per normalized account/IP identity), and reset completion (5 per 15 minutes per opaque user/IP identity). No raw email or IP is persisted. Public credential and recovery responses remain generic.

**Verification result (updated 2026-08-26): LOCAL/STATIC GREEN; NEW-USER FIRST LOGIN DEFERRED.** The confirmed anonymous Phase 13B diagnostic account was revalidated as non-allowlisted, Profile-less, and unreferenced before deletion. Exactly one approved remaining account was provisioned without exposing or persisting its bootstrap password; live password recovery/reset completed, but that account did not establish an application session and still has no Profile, so first-login Profile bootstrap plus reload/new-tab/logout proof is explicitly deferred rather than passed. Sanitized provider state is two allowlisted Auth users, one Profile, one remaining eligible approved account, and empty business tables. Final gates pass: focused Appwrite/auth tests 69/69; full Vitest 555/555 across 71 files; architecture 26/26; ESLint zero errors/warnings; TypeScript and production build green; dependency audit zero vulnerabilities; `git diff --check` green; built-client secret audit green across 53 static files with zero configured-secret values or server-secret names. The previously completed existing-account live matrix remains 9/9 in Chromium, 9/9 in Firefox, and 9/9 in WebKit with zero serious/critical Axe findings and zero console/page/hydration errors; remote `deleteSession("current")` was proven by rejected reuse of the pre-logout cookie. Known and unknown recovery responses remained enumeration-safe; registration, verification, and email-edit routes remain absent. Phase 13B remains uncommitted; the final approved account remains unprovisioned; Phase 13C is unauthorized and has not started.

### Phase 14 - Integration and security QA

Test production-like multi-user concurrency, authorization bypass attempts, card/receipt privacy, settlement duplication, atomicity, data migration, recovery, and audit history. **Reasoning: high** due adversarial security and financial integrity. Exit: no open critical/high findings and all end-to-end financial invariants pass.

### Phase 15 - Deployment

Choose only zero-cost approved infrastructure, configure environments/secrets, CI quality gates, deployment, monitoring within free limits, and rollback/runbook documentation. **Reasoning: medium-high** because mechanics are established but release safety matters. Exit: deployed smoke tests and explicit release approval.

## Current authorization boundary

All approved local/provider-independent work through Pre-Production Business Logic Hardening H1-H8 is complete and approved at the `pre-appwrite-local-v1` checkpoint. Appwrite has not been implemented and remains explicitly blocked pending separate Phase 13 authorization. Do not begin Appwrite, scheduled receipt cleanup, or any later production phase without separate authorization.

### Pre-Production Business Logic Hardening - local/provider-independent

**Authorization/result:** H1-H8 local/provider-independent implementation was approved on 2026-08-22 and is now complete and approved for the pre-Appwrite checkpoint. Appwrite production implementation remains separately gated.

- **H1 complete:** authoritative injected Clock -> `Asia/Dhaka` business date; same-day allowed; future Create/financial Edit rejected; legacy future records preserved under the approved compatibility rule.
- **H2 complete:** Expense revision OCC, no-op semantics, deterministic precedence, strict IndexedDB v4 -> v5 migration, command-outcome store, and Receipt indexes.
- **H3 complete:** transaction-time backdated Create/Edit challenge with equality warning, canonical intent binding, one retained command ID, stale/forged/new-boundary rejection, and derived `Added after settlement` context.
- **H4 complete:** creator/historical-uploader private reads, creator-only future management, generic no-ID attachment projection, Leader non-override, repository read gating, and privacy-safe UI.
- **H5 complete:** configurable 3 available/Expense, 10 MiB/file, 50 MiB/uploader, 1,000,000,000-byte project budget, 800,000,000-byte warning, transactional available-content accounting, and reservation-aware policy contracts.
- **H6 complete:** provider-independent command ID/type/intent/outcome model and atomic IndexedDB outcome ledger for Create Expense, Create Household, Send Join Request, Create Pending Settlement, Upload Receipt, and Create Card; replay authorization and actor isolation retained.
- **H7 complete:** application business-date default/max, authoritative warning dialog/retry, OCC reload/review state, informational late-recording indicator, private Receipt presentation, count/quota context, and retained command IDs for protected UI actions.
- **H8 complete:** full adversarial regression and release-style gates pass. Vitest is green at 469/469 across 60 files; architecture guards are 16/16; Playwright is 69/69 across Chromium, Firefox, and WebKit; the responsive matrix and Axe serious/critical checks pass; lint, TypeScript, production build, dependency audit, `git diff --check`, and manual financial/privacy/date/Appwrite-scope audits pass. Financial calculations, recommendations, Settlement history/locks, Receipt retention, Card privacy, Household management, Dashboard, and Reports remain intact. No Appwrite implementation was introduced.

**Phase 13 boundary:** only interfaces/semantics are prepared locally. Do not add Appwrite SDKs, Functions, Storage permissions/uploads, server timestamps, provider transactions, organization usage queries, scheduled workers, HMAC signing, or orphan reconciliation in this phase. The 10 MiB production upload transport remains a Phase 13 proof/redesign decision.

### Post-checkpoint record (2026-08-22, owner-approved)

These items post-date the `pre-appwrite-local-v1` checkpoint and supersede older statements above where they conflict (notably the Phase 10 "no Household rename UI" clarification). Full details live in `PROJECT_STATE.md` and `REQUIREMENTS.md`.

- **Pre-production correction pass:** atomic `updateCurrentProfile` (transaction-time OCC + email uniqueness), typed `INVALID_HOUSEHOLD_CODE`, multi-reason eligibility errors via `DomainError.details`, removal of the dead rename/update-household persistence surface, retention failure diagnostics, and an owner-approved local bootstrap receipt-retention sweep.
- **House name rename feature:** leader-only rename dialog with a purpose-specific atomic operation (commit-time authority recheck, unchanged-name suppression, sanitized `renamed` audit).
- **Dashboard month-selector completeness fix:** current local calendar month is always selectable; Dashboard selection persists across client-side navigation per identity+household while fresh loads still default to the current month.
- **Pre-Appwrite code-health/performance optimization pass (owner-scoped):** dead-file/barrel/export removal, Recharts deferred out of the initial Dashboard/Report route graphs behind the sanctioned chart boundary, the pending-receipt object-URL commit-gap fix with early revoke for staged removals, shared `DatabaseSource` type, documentation reconciliation, and vacuous route-assertion correction. No financial, permission, privacy, OCC, idempotency, retention, or persistence-semantics behavior changed.
- **Production mobile logout hotfix v1.0.1 (2026-08-30, owner-approved):** commit `e9695e6` adds a separated final `Log Out` action to the existing mobile More sheet, reusing the unchanged desktop runtime `signOut` operation with a 48px target and repeat-submission guard. Appwrite Sites deployment `6a93ec6d327cd27f90ff` passed production mobile smoke at the unchanged generated origin: the action is final, separated, non-clipped, and redirects to `/login`; `/dashboard` re-gates afterward. Authentication semantics, financial behavior, backend/schema, persistence, permissions, and privacy are unchanged. Credential-backed automated logout is accurately deferred because `.env.auth-smoke.local` is absent. Release tag: `v1.0.1`. No new development phase was created.
