# Project State

## Snapshot

- Project: House Finance Tracker
- Stage: complete approved local application at the `pre-appwrite-local-v1` handoff checkpoint, plus post-checkpoint owner-approved work: pre-production hardening corrections (H1-H8), the provider-independent Receipt Retention Foundation, Confirmed Settlement Financial Lock, review-driven correction pass, House name rename feature, Dashboard month-selector completeness fix, and the scoped pre-Appwrite code-health/performance optimization pass
- Application implementation: foundation, pure domain engines, application services/ports, IndexedDB v5 persistence/migrations, responsive application shell/design system, household onboarding and management, complete local Expense/receipt and Settlement workflows, owner-private Cards management, source-derived Dashboard analytics, Monthly Reports, provider-independent receipt retention, transactionally enforced settled-history Expense locks, Expense OCC, business-date/backdated policies, private Receipt projections/admission, and protected-create idempotency semantics
- Local MVP: complete and approved for handoff, including local session, household onboarding, active-member identity, protected House Code, Join Request review, Leave/Remove/Transfer/Delete workflows, exact financial gates, Expense/Receipt/Card/Settlement workflows, current balances/recommendations, Dashboard analytics, Monthly Reports, responsive behavior, accessibility, and adversarial regression; the browser bootstrap starts with only the four development identities and no sample household or financial records; production Appwrite/backend/deployment work remains unimplemented
- Regression gate: full Vitest is green at 469 tests across 60 files; architecture guards pass 16 cases; Playwright passes 69/69 across Chromium, Firefox, and WebKit, including the responsive matrix and zero serious/critical Axe findings. Lint, TypeScript, production build, dependency audit, financial/privacy/date/Appwrite-scope audits, and `git diff --check` are green. Confirmed-settlement and hardening coverage includes future-date/Dhaka boundaries, migration/OCC conflicts, stale financial Save/Delete rollback, backdated challenge/reconfirmation, Receipt privacy/admission/terminal-capacity release, protected-create replay isolation, and unchanged financial calculations/recommendations/history.
- Appwrite integration: not implemented; Phase 13 still requires separate authorization and must preserve the provider-independent domain/application contracts and every frozen security/financial rule
- Requirements/design status: approved frozen context received and reconciled; the four canonical Figma desktop frames were applied as the UI Parity source of truth; the frozen Receipt Retention, Confirmed Settlement Financial Lock, and Pre-Production Business Logic Hardening requirements were approved on 2026-08-22
- Active implementation plan: all approved local work through hardening H1-H8 is complete and approved for the Git handoff checkpoint; there is no active implementation phase; Appwrite remains gated to separately authorized Phase 13 work
- Current presentation correction (2026-08-22): Expenses now presents the no-filter Payment Method trigger as `Payment Method` while preserving the `all` query sentinel; Add/Edit Expense now share the reusable Radix Popover date-only control, with canonical `YYYY-MM-DD` values and no timezone serialization; Dashboard Spending Trend now spans the full analytics content width, Payment Mix sits in the supporting row with Housemate Balances and Recent Expenses, and the chart only retains contained scrolling below the narrow-mobile fit boundary. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.
- Corrective financial-integrity result (2026-08-22): same-Household Confirmed Settlement history now derives an unpersisted latest boundary and financially locks every Expense with `createdAt` at or before it. The canonical transaction rereads current Expense identity/history, permissions/memberships, Settlements, and private Card association before any write; stale financial Save/Delete attempts roll back completely. Presentation keeps authorized Name/receipt work available, private Card identities remain opaque, and no migration or Appwrite implementation was introduced.
- Sidebar/mobile navigation presentation correction (2026-08-22): desktop navigation now has an accessible smooth collapse/expand control that transitions between the full sidebar and an icon rail while keeping navigation, account, and development controls available. In the collapsed rail, the expand control is revealed in place over the logo on hover/focus and never protrudes into dashboard content. Mobile/tablet bottom navigation is now icon-only with centered controls, accessible names, and hover titles; its routing and More sheet behavior are unchanged.
- Attention-badge and filter-sizing presentation correction (2026-08-22): leader Pending Join Request counts now badge the desktop Household item and the mobile More trigger plus its Household sheet row, derived from the existing household access projection per the frozen badge rule. The `>=1400px` Expenses filter grid uses content-driven `minmax()` tracks and an intrinsic Clear Filters button instead of fixed pixel columns that truncated labels. The fixed mobile nav bar and root `<body>` carry scoped `suppressHydrationWarning` because this machine's force-loaded browser extensions inject pre-hydration DOM attributes (`style="translate: ..."`, `cz-shortcut-listen`) that React cannot reconcile. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed. Vitest 473/473 across 60 files; focused Chromium Playwright suites green including a seeded end-to-end check of both badges and measured zero filter truncation at 1440px.
- Expense-form panel sizing correction (2026-08-22): the `>=1400px` canonical block no longer pins the Add/Edit Expense details, receipts, or split panels to fixed pixel heights. Panels now size from content, so Cash collapses naturally while Card selection, receipt uploads, and participant grids expand inside their sections instead of drawing past the surface border. Measured at 1440x1024: details panel 220px with Cash versus 306px with Card and zero descendant overflow in every state. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.
- Sidebar collapse control restyle (2026-08-22): both sidebar toggle states are now restrained ghost icon buttons with muted icons and a subtle secondary hover square at the 12px radius. The collapsed expand reveal keeps its frozen hover/focus-over-the-logo behavior but crossfades opacity only inside the logo's exact 40px rectangle, replacing the previous solid dark pill whose 20px `rounded-xl` radius read as a circle and whose scale animation drifted upward. Measured in place: zero top/left offset versus the logo, transparent background, no shadow, no transform. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.
- Dashboard Member Contributions module (2026-08-22): added a fourth summary card listing each selected-month payer with their exact paid total, reusing the Monthly Report contributions engine with the same reconciliation invariant and deterministic paid-descending ordering; former members are labeled and the card reflows 4-up/2x2/stacked across breakpoints. Owner-approved requirement addition recorded in `REQUIREMENTS.md`. No persisted aggregates, new financial rules, or privacy changes. Vitest green at 476/476 across 61 files; analytics Chromium e2e passed 9/9 including responsive and Axe checks; seeded browser verification measured four cards on one row at 1440px with zero overflow and exact totals.
- Card Designs presentation change (2026-08-22): Card create/edit now offers six realistic payment-card designs (Red, Yellow, Black, Blue, Green, Orange) rendered as live previews with the entered Card name, owner display name, Debit/Credit badge, and decorative demo number/expiry constants that are never stored or submitted; My Cards renders each owned Card as its full design. The storage vocabulary widened additively with six new identifiers while all legacy identifiers stay readable forever, private historical snapshots remain untouched, no schema-version bump was needed, and V1 color migration is anchored to `LEGACY_CARD_COLOR_IDS` so historical determinism survives vocabulary growth. Owner-approved requirement change recorded in `REQUIREMENTS.md`. Vitest green at 476/476 across 61 files including a design-id persistence round trip and demo-value isolation assertions; cards Chromium e2e passed 4/4 including Axe serious/critical checks on the new previews.
- Pre-production correction pass (2026-08-22): owner-directed review findings fixed without touching financial engines. Profile updates now commit through a new atomic `updateCurrentProfile` operation that rechecks `updatedAt` OCC and transaction-time `emailKey` uniqueness inside one IndexedDB transaction (the unused non-atomic profile update/find paths were removed from the repository port); malformed household codes now fail with a distinct `INVALID_HOUSEHOLD_CODE` application error while genuine code collisions remain `CONFLICT`; multi-blocker membership eligibility failures carry every reason on the thrown `DomainError.details` while keeping the first reason as the primary code; the dead `renameHousehold` service method and its orphaned `updateHousehold`/household `updateDetails` persistence operations were removed per the frozen no-rename scope; receipt retention summaries report capped per-receipt failure details; and an owner-approved local bootstrap retention sweep (see the approved amendment in `REQUIREMENTS.md`) opportunistically expires eligible receipt content during runtime acquisition through a privileged infrastructure trigger with non-fatal diagnostics logging. Card lifecycle audit events remain intentionally omitted per the Phase 9 lesson, reaffirmed by the owner during this review. Vitest green at 483/483 across 61 files (new coverage: atomic profile uniqueness/OCC, typed house-code errors, multi-reason eligibility details, retention sweep behavior); architecture guards 16/16; lint, TypeScript, and production build green; no domain money, balance, lock, privacy, or migration behavior changed.
- House name rename feature (2026-08-22): owner-approved requirement change recorded in `REQUIREMENTS.md` supersedes the earlier no-rename statement. The active Household view offers a leader-only Rename control opening a React Hook Form + Zod dialog seeded with the current name; empty-after-trim submissions are blocked client-side, and the authoritative path resolves the active household, gates on active Leader (`NOT_FOUND` otherwise), short-circuits unchanged names without any write or audit, and commits through a new purpose-specific atomic `renameHousehold` operation that transactionally re-reads the Household (alive), revalidates active-Leader authority and audit consistency at commit, persists only `{name, updatedAt}` through `assertHousehold`, and appends one sanitized `renamed` audit event naming field names only. Members and former members receive no control and cannot invoke the operation. Vitest green at 485/485 across 61 files including integration coverage (rename+audit+timestamp, member/outsider rejection, no-op suppression, blank-name rejection) and presentation coverage (leader-only visibility, dialog validation, commit); household-management and household-onboarding Chromium e2e suites passed 15/15 against a locally started dev server, including a new end-to-end rename test verifying in-place name change, empty-name blocking, member absence of the control, and cross-identity persistence. Lint, TypeScript, and production build green; no financial, privacy, permission-beyond-leadership, or migration behavior changed.
- Dashboard month-selector completeness correction (2026-08-22): owner-reported defect fixed where selecting a past month with an expense-free current month removed the current month from the selector options entirely (options derived only from selectedMonth plus expense months), leaving a full reload as the only way back. `monthOptions` now always unions the viewer's current local calendar month, threaded as an explicit parameter from `analytics-service.ts` into both page-view builders because the architecture guard keeps `new Date()` out of analytics aggregation files; the Monthly Report inherits the same completeness fix. The Dashboard additionally retains the chosen month across client-side navigation through a session-scoped store keyed per identity+household, implemented with a keyed inner component remount instead of setState-in-effect; a fresh load still defaults to the current local month per frozen Phase 11 behavior. Approved clarification recorded in `REQUIREMENTS.md`. Vitest green at 487/487 across 62 files including new option-completeness unit assertions for both selectors; analytics Chromium e2e passed 10/10 against the locally running dev server including a new regression test proving August stays selectable after selecting July and that in-session navigation preserves the chosen month. TypeScript, lint, and production build green; no financial calculations, balances, permissions, privacy, or persistence behavior changed.
- Pre-Appwrite optimization pass (2026-08-22, owner-scoped): dead-code/hygiene subset executed after reference verification — removed three orphan components (`financial-status.tsx`, `money-input.tsx`, `feature-placeholder.tsx`), three inert barrels (`application/index.ts`, `infrastructure/index.ts`, `presentation/index.ts`; the active `application/repositories` barrel was kept), the never-referenced `ReceiptStorageReservation`, dead `LegacyCardColorId`/`isCardDesignId` exports, and un-exported internal demo constants; `expense-financial-input.schema.ts` plus its test were removed after the mandated investigation confirmed zero production responsibility, supersession by `expense-form.schema.ts` plus authoritative application/domain validation, and a payload shape that never matched the real branded-value command contract (git history preserves it for Phase 13 reuse); `sumPoisha` and its overflow coverage were deliberately kept; unused CSS tokens were retained per instruction; a generic `*.log` ignore rule was added. Recharts now loads dynamically behind the sanctioned chart boundary pair (`analytics-charts.client.tsx` wrapper -> lazily imported `analytics-charts-recharts.client.tsx`, architecture guard updated to enforce both the single-import rule and the lazy pairing): measured against a served production build, the 342 KB Recharts chunk is absent from the initial HTML script set of `/dashboard` (18 chunks, 1,248 KB) and `/reports/monthly`, previously shipping statically as the largest 358 KB chunk. The pending-receipt object-URL commit-gap leak was fixed by synchronous ref ownership at mutation points (also removing an impure setState updater), staged removals now revoke their preview URLs early (safe because the render filters them out immediately and Cancel navigates away), and the three identical infrastructure-local `DatabaseSource` definitions were consolidated into `indexeddb/database.ts`. OPT-2 micro-optimizations were evaluated and deliberately skipped as negligible under the owner's evidence bar. Documentation reconciled: README rewritten to current reality, ACTIVE_PLAN status header refreshed with a post-checkpoint record section, PROJECT_STATE stage line updated, and the vacuous `/settlements/history` gating assertion replaced with the real `/settlements` route; the `as CalendarMonth` cast now uses the validated `calendarMonth()` constructor. Full regression: Vitest 478/478 (net -9 from the deleted obsolete schema test), architecture guards 16/16, lint/typecheck/production build green, `npm audit` zero vulnerabilities, `git diff --check` clean, full Chromium Playwright 69/69 including Axe and the responsive matrix, Firefox cross-browser smoke green; the WebKit smoke is blocked by a local Playwright-WebKit runtime failure that cannot open even `about:blank` (verified with an app-independent probe) and is unrelated to application changes. No business behavior, financial output, privacy projection, persistence schema, or Appwrite introduction occurred; all optimization changes remain uncommitted for review.

- UI/UX audit fixes (2026-08-22): owner-approved audit remediation executed across B1-B10 with zero business/financial/privacy/persistence changes. Design tokens: the three frozen-hex contradictions (--text-muted, --danger-foreground, --warning-foreground) were retained as sanctioned AA-contrast deviations with explanatory comments (frozen muted/danger/warning values fail WCAG AA at their usage sizes); .page-title aligned to the frozen 32/40 display step. Type/rhythm normalization: 18px and 22px financial totals moved to the 20px H3 step, gap-[18px] to 16px, radii 14->12 / 9->8 / 10->12 including the Button component radius, sidebar toggle enlarged 36->40px, and the invisible sr-only receipt file-input tab stop gained a focus-within ring. UX states: Expenses error state gained retry plus distinct no-expenses vs no-matches empties with an inline Clear Filters action and a polite live result count; Add/Edit Expense load failure now renders a full ErrorState-with-retry instead of a dead editable form; a shared userErrorMessage sanitizer replaced raw error.message surfacing in Settlements, Join, Create, and form paths; receipt preview failures became visible in the edit form matching Details; invalid report month links now show a warning notice over the fallback month; Dashboard pre-ready shell renders a loading skeleton; the deleted-expense edit notice became a styled status banner. Copy/destructive consistency: rename dialog now uses Save Changes, settlements cancel trigger renamed Cancel Claim (spec updated), Try Again casing unified, Archive confirmation is no longer destructive-styled, the backdated dialog disables Cancel while saving, remove-card focus restore uses the isConnected guard, and the expense-form submit gained aria-busy plus spinner matching all other forms. A11y: settlement table headers got scope=col, join-request rows show localized dates with <time> elements replacing raw ISO, expense dates expose dateTime attributes, activity-feed timestamps pin en-GB instead of device locale, and settlement dialogs render amounts as tabular numerals. Mobile attention badges switched from danger-red to desktop brand styling, the More sheet gained the missing settlement-action badge, and the More trigger label announces both counts. Deliberately skipped per audit judgment: CSS token purge beyond comments, micro-type on chart/avatar/badge graphics, text-sm body line-height normalization, input maxLength/money pattern (would invent limits), and toast politeness override (inline/live regions already carry critical errors). Verification: Vitest 478/478, lint/typecheck/production build green, Chromium Playwright 69/69 incl. Axe and responsive matrix, Firefox smoke green, git diff --check clean. WebKit remains unavailable by owner decision: Windows Smart App Control blocks the unsigned Playwright WebKit child binary (verified root cause via PrintDeps Application-Control block and SAC state query); Firefox and Chromium are the documented verified browsers on this machine.
- Dashboard UX polish pass (2026-08-23, owner-requested; scope in `docs/ai/work/UI_UX_POLISH_PLAN.md`): presentation-only refinements — Dashboard month switching keeps the previous view dimmed with an inline updating indicator instead of swapping to a loading skeleton (stale-while-revalidate); fixed heights became floors on summary cards, Recent Expenses, and all `>=1400px` expense panels so content grows instead of clipping; 24 arbitrary text-size utilities consolidated into font-size-only theme tokens (`text-mini/text-fine/text-row/text-stat`); empty states gained icon-and-CTA treatments linking to `/expenses/new`; Housemate Balances, Recent Expenses, and Payment Mix gained mobile-only collapse toggles (default expanded); MonthSelector gained clamped Previous/Next steppers backed by a new pure `nextCalendarMonth`; daily bars now distinguish weekends and mark today during the current month (replacing the unconditional last-day highlight) and non-compact Payment Mix slices show rounded percent labels beside the exact textual rows; Spent/Outstanding values animate on change with reduced-motion safety; and the sidebar dev-tools strip hides when the composition root omits development identities (environment branching stays exclusively in the composition root per the architecture guard). No domain, application-service, repository, persistence, permission, privacy, financial, analytics-aggregation, or route behavior changed. Vitest green at 486/486 across 61 files; lint and TypeScript clean; analytics Chromium e2e passed 10/10 including responsive geometry and zero serious/critical Axe findings.
- Phase 12 re-hardening and Local MVP Freeze candidate (2026-08-23): owner-directed Phase 12 re-execution over all post-checkpoint drift (rename, Card Designs, Member Contributions, retention sweep, optimization pass, UI/UX polish). One serious defect found and fixed — opacity-fade enter animations on dashboard metric values failed WCAG contrast while running and broke the Axe stabilization gate; motion is now transform-only. Two touch-target gaps hardened (month steppers to 44px, mobile collapse buttons to 40px). Audits reconfirmed atomic persistence/OCC/idempotency coverage, 15 double-submit guard sites, zero web-storage leakage, privacy projections under cross-identity e2e, and 360–1440px responsive geometry. Freeze matrix green: Vitest 486/486 (61 files, architecture guards included), lint/typecheck/build clean, npm audit zero vulnerabilities, git diff --check clean, Playwright Chromium 69/69, Firefox/WebKit smoke 2/2. Local MVP Freeze recommended for owner approval; Phase 13 remains gated.
- Phase 13 status (2026-08-23): owner approved the Rev 2 small-in-house architecture plan (`PHASE_13_PLAN.md`) with final corrections (4-member cap, closed fail-closed allowlist, guard rows over filtered indexes, BigInt money, no persisted lock state, Auth email as sole authoritative email, deletion bounded <=15 ops). **Slice 13A complete and uncommitted pending review:** Appwrite server SDK + `src/infrastructure/appwrite/` foundation (config with separated runtime/bootstrap credentials, fail-closed registration allowlist, SHA-256-derived guard/outcome IDs, declarative 14-table schema + private bucket + maintenance Function skeleton, non-destructive bootstrap planner/dry-run-first applier/CLI) plus new architecture containment guards. No cloud resources touched; production composition still IndexedDB-only; zero product behavior change. Vitest 517/517 (65 files); lint/typecheck/build/audit/diff-check clean. 13B not authorized yet.
## Confirmed direction

The product is a shared household expense tracker with a local/mock MVP, exact integer-poisha finance engine, repository-separated architecture, and the frozen stack and UX system in `REQUIREMENTS.md`.

## Current gate

**LOCAL MVP = FROZEN (owner-approved 2026-08-23). PHASE 12 = COMPLETE. APPWRITE = NOT IMPLEMENTED. PHASE 13 = NEXT.**

The complete local application at this checkpoint is the authoritative pre-Appwrite Local MVP baseline. Product behavior, financial rules, privacy, UI/UX, responsive behavior, and accessibility behavior are frozen from here onward; Phase 13 must not casually rewrite them when the persistence/authentication provider changes. The intentionally deferred production concerns (trusted server time, signed confirmations, cross-device OCC, Storage quotas/reservations, scheduled retention Function, production auth/session enforcement, anti-enumeration/rate limiting, real server permissions) belong to Phase 13 and are not local defects.

## Next safe action

The next coding agent should begin Phase 13 - Appwrite Integration with an architecture/planning document against this frozen baseline before writing code. Do not implement Phase 13 behavior without that plan, and do not reopen frozen local rules merely because the provider changes.

## Pre-Production Business Logic Hardening status (2026-08-22)

- H1-H8 are complete and approved at the local checkpoint: Dhaka authoritative Expense business date/future rejection, Expense revision OCC, IndexedDB v5, backdated Create/Edit confirmation, derived late-recording context, creator/uploader Receipt privacy, creator-only management, three-file/50 MiB/1 GB admission, command outcomes/idempotent protected creates, integrated UI states, and full adversarial regression.
- No financial allocator, balance rule, recommendation algorithm, Confirmed Settlement record, receipt-retention lifecycle, Card privacy rule, Household-management rule, report grouping rule, or historical Expense Date was rewritten.
- Real malicious-client enforcement remains unavailable locally. Phase 13 still owns trusted server time, signed confirmation, Appwrite transactions/permissions, cross-device OCC, Storage reservations/upload saga, provider usage reconciliation, orphan cleanup, and scheduled retention.
- H8 exit evidence: Vitest 469/469 across 60 files; architecture 16/16; Playwright 69/69 across Chromium, Firefox, and WebKit; responsive and Axe serious/critical checks green; lint, TypeScript, production build, dependency audit, `git diff --check`, and manual financial/privacy/date/Appwrite-scope audits green. No Appwrite implementation was added.
