# Project State

## Snapshot

- Project: House Finance Tracker
- Stage: complete approved local application at the `pre-appwrite-local-v1` handoff checkpoint; Phase 11, UI parity/stabilization corrections, the provider-independent Receipt Retention Foundation, the Confirmed Settlement Financial Lock, and Pre-Production Business Logic Hardening H1-H8 are complete
- Application implementation: foundation, pure domain engines, application services/ports, IndexedDB v5 persistence/migrations, responsive application shell/design system, household onboarding and management, complete local Expense/receipt and Settlement workflows, owner-private Cards management, source-derived Dashboard analytics, Monthly Reports, provider-independent receipt retention, transactionally enforced settled-history Expense locks, Expense OCC, business-date/backdated policies, private Receipt projections/admission, and protected-create idempotency semantics
- Local MVP: complete and approved for handoff, including local session, household onboarding, active-member identity, protected House Code, Join Request review, Leave/Remove/Transfer/Delete workflows, exact financial gates, Expense/Receipt/Card/Settlement workflows, current balances/recommendations, Dashboard analytics, Monthly Reports, responsive behavior, accessibility, and adversarial regression; the browser bootstrap starts with only the four development identities and no sample household or financial records; production Appwrite/backend/deployment work remains unimplemented
- Regression gate: full Vitest is green at 469 tests across 60 files; architecture guards pass 16 cases; Playwright passes 69/69 across Chromium, Firefox, and WebKit, including the responsive matrix and zero serious/critical Axe findings. Lint, TypeScript, production build, dependency audit, financial/privacy/date/Appwrite-scope audits, and `git diff --check` are green. Confirmed-settlement and hardening coverage includes future-date/Dhaka boundaries, migration/OCC conflicts, stale financial Save/Delete rollback, backdated challenge/reconfirmation, Receipt privacy/admission/terminal-capacity release, protected-create replay isolation, and unchanged financial calculations/recommendations/history.
- Appwrite integration: not implemented; Phase 13 still requires separate authorization and must preserve the provider-independent domain/application contracts and every frozen security/financial rule
- Requirements/design status: approved frozen context received and reconciled; the four canonical Figma desktop frames were applied as the UI Parity source of truth; the frozen Receipt Retention, Confirmed Settlement Financial Lock, and Pre-Production Business Logic Hardening requirements were approved on 2026-08-22
- Active implementation plan: all approved local work through hardening H1-H8 is complete and approved for the Git handoff checkpoint; there is no active implementation phase; Appwrite remains gated to separately authorized Phase 13 work
- Current presentation correction (2026-08-22): Expenses now presents the no-filter Payment Method trigger as `Payment Method` while preserving the `all` query sentinel; Add/Edit Expense now share the reusable Radix Popover date-only control, with canonical `YYYY-MM-DD` values and no timezone serialization; Dashboard Spending Trend now spans the full analytics content width, Payment Mix sits in the supporting row with Housemate Balances and Recent Expenses, and the chart only retains contained scrolling below the narrow-mobile fit boundary. No domain, application-service, repository, persistence, permission, privacy, financial, analytics, or route behavior changed.
- Corrective financial-integrity result (2026-08-22): same-Household Confirmed Settlement history now derives an unpersisted latest boundary and financially locks every Expense with `createdAt` at or before it. The canonical transaction rereads current Expense identity/history, permissions/memberships, Settlements, and private Card association before any write; stale financial Save/Delete attempts roll back completely. Presentation keeps authorized Name/receipt work available, private Card identities remain opaque, and no migration or Appwrite implementation was introduced.
- Sidebar/mobile navigation presentation correction (2026-08-22): desktop navigation now has an accessible smooth collapse/expand control that transitions between the full sidebar and an icon rail while keeping navigation, account, and development controls available. In the collapsed rail, the expand control is revealed in place over the logo on hover/focus and never protrudes into dashboard content. Mobile/tablet bottom navigation is now icon-only with centered controls, accessible names, and hover titles; its routing and More sheet behavior are unchanged.

## Confirmed direction

The product is a shared household expense tracker with a local/mock MVP, exact integer-poisha finance engine, repository-separated architecture, and the frozen stack and UX system in `REQUIREMENTS.md`.

## Current gate

No local implementation work remains active. Preserve this checkpoint and do not begin Appwrite without explicit Phase 13 authorization.

## Next safe action

The next coding agent should begin by reading the frozen AIDOS documents and inspecting the `pre-appwrite-local-v1` checkpoint. The next permissible development step is a separately authorized Phase 13 Appwrite plan/integration; do not infer that authorization from the roadmap.

## Pre-Production Business Logic Hardening status (2026-08-22)

- H1-H8 are complete and approved at the local checkpoint: Dhaka authoritative Expense business date/future rejection, Expense revision OCC, IndexedDB v5, backdated Create/Edit confirmation, derived late-recording context, creator/uploader Receipt privacy, creator-only management, three-file/50 MiB/1 GB admission, command outcomes/idempotent protected creates, integrated UI states, and full adversarial regression.
- No financial allocator, balance rule, recommendation algorithm, Confirmed Settlement record, receipt-retention lifecycle, Card privacy rule, Household-management rule, report grouping rule, or historical Expense Date was rewritten.
- Real malicious-client enforcement remains unavailable locally. Phase 13 still owns trusted server time, signed confirmation, Appwrite transactions/permissions, cross-device OCC, Storage reservations/upload saga, provider usage reconciliation, orphan cleanup, and scheduled retention.
- H8 exit evidence: Vitest 469/469 across 60 files; architecture 16/16; Playwright 69/69 across Chromium, Firefox, and WebKit; responsive and Axe serious/critical checks green; lint, TypeScript, production build, dependency audit, `git diff --check`, and manual financial/privacy/date/Appwrite-scope audits green. No Appwrite implementation was added.
